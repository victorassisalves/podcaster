FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml .
# Copy src directory so setuptools can find the package during installation
COPY src ./src
RUN pip install .

COPY . .

EXPOSE 8000

CMD ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8000"]
